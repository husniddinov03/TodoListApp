// 0,1,1,2,3,5,8,13


let numbersFunc =()=>{
    const numbers = [0, 1]

    const numPlus = numbers.join("+")
    
    // numbers.forEach(number => {
    //     numbers
    // });
    console.log(numPlus);
}

numbersFunc()